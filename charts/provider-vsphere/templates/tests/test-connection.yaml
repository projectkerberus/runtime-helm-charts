apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "provider-vsphere.fullname" . }}-test-connection"
  labels:
    {{- include "provider-vsphere.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "provider-vsphere.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
